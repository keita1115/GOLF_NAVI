   <!--検索機能-->
 <div class="container mt-5">
  <div class="row">
 　　<%= form_with url: searches_path, method: :get, local: true do |f| %>
　   <%= f.text_field :keyword, value: @keyword %>
  　  <%= f.submit "検索" %>
    <% end %>
  </div>
    <div class="post-list">
    <% @courses.each do |course| %>
    <% end %>
   </div>
 </div>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-3">
      <!--部分テンプレート-->
    <%= render "public/courses/genre_search", genres: @genres %>
    </div>
    <div class="col-8 ml-2">
      <!--countメソッドの使い方-->
      <h3 class="mb-4">ゴルフ場一覧 </h3>

      <!--ゴルフ場一覧-->
      <div class="contents" style="display:flex; flex-wrap:wrap;">
        <% @courses.each do |course| %>
          <div class="mr-4 mt-4">
            <%= link_to course_path(course.id) do %>
             <% if course.image.attached? %>
               <%= image_tag course.image, size:"200x120" %>
             <% else %>
               <%= image_tag "e_others_501.jpeg" %>
             <% end %>
             <% end %>
            <p class="text-center"><strong><%= course.title %></strong></p>
          </div>
        <% end %>
      </div>
       <p class="text-center"><strong> <%= paginate @courses %></strong></p>
    </div>
  </div>
</div>